services:
  agregator:
    build:
      dockerfile: docker/Dockerfile
      context: ..
    image: voice_assistant:agregator
    env_file:
      - .docker.env
    volumes:
      - ../data:/data
      - ../data:/logs

#  postgres:
#    image: postgres:16
#    environment:
#      POSTGRES_USER: langflow
#      POSTGRES_PASSWORD: langflow
#      POSTGRES_DB: langflow
##    ports:
##      - "5432:5432"
#    volumes:
#      - langflow-postgres:/var/lib/postgresql/data
#
#volumes:
#  langflow-postgres:
#  langflow-data:
